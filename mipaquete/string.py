
def minuscula(texto : str) -> str:
    """La función devuelve la cadena pasada por parámetro en mayúscula"""
    return texto.lower()

def mayuscula(texto :str) -> str:
    """La función devuelve la cadena pasada por parámetro en minúscula"""
    return texto.upper()